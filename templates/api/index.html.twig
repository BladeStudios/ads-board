{% extends 'base.html.twig' %}

{% block title %}API - AdsBoard{% endblock %}

{% block body %}
{{ parent() }}
<main>
    <h1 class="h1">API</h1>
    <article>
        <p>API udostępnia następujące możliwości:
        <ul>
            <li>Wyświetlenie ogłoszenia o podanym {id}
                <pre>GET {{ path('api_ad_display', {id: '{id}'}) | replace({'%7B':'{', '%7D':'}'}) }}</pre>
            </li>
            <li>Dodawanie ogłoszenia o podanej nazwie {name}, cenie {price} i opisie {description}
                <pre>POST {{ path('api_ad_add') }}?name={name}&price={price}&description={description}</pre>
            </li>
            <li>Wyszukiwanie ogłoszeń
                <pre>GET //TODO</pre>
            </li>
        </ul>
        Dane zwracane są w formacie json.
        </p>
    </article>
</main>
{% endblock %}